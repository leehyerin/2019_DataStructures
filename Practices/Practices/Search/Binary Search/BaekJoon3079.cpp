#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
	unsigned int N, M; //N 심사대 수, M 사람 수  (1 ≤ N ≤ 100,000, 1 ≤ M ≤ 1,000,000,000)
	cin >> N >> M;
	vector<unsigned int> T(N);

	unsigned int t;
	for (unsigned int i = 0; i < N; ++i)
	{
		T.push_back(t);
	}


	return 0;
}