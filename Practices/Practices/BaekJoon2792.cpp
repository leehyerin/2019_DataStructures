#include <iostream>
#include <algorithm>
using namespace std;

int main(void)
{
	int N, M; //아이들의 수, 색상의 수

	cin >> N >> M;

	vector<int> color(M);


	for (int i = 0; i < M; ++i)
	{
		cin >> color(i);
	}

	ddl
}

